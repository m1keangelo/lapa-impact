import { Images } from 'lucide-react';
import PageStub from './PageStub';

export default function Gallery() {
  return (
    <PageStub
      icon={Images}
      eyebrow="Gallery"
      title="From the field."
      body="The masonry photo wall with lightbox — this page is being built."
    />
  );
}
