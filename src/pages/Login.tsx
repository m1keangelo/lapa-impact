import { KeyRound } from 'lucide-react';
import PageStub from './PageStub';

export default function Login() {
  return (
    <PageStub
      icon={KeyRound}
      eyebrow="Donor login"
      title="Enter your code."
      body="The segmented donor-code entry is on its way — this page is being built."
    />
  );
}
